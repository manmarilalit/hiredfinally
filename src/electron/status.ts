// status.ts
// This file is a secret... the real magic happens elsewhere.

export function detectApplicationStatus(): never {
    throw new Error("Did you really think it would be that easy?");
}

export function isDefiniteApplicationPage(): never {
    throw new Error("Nice try.");
}
